# 마케팅 활용동의 획득 실적 대시보드 — 작업 지침

한화손보 고객상담파트용 단일 파일 대시보드. 사용자는 고객상담파트 담당자이며,
**엑셀만 전달하고 코드는 직접 수정하지 않는다.** 데이터 반영은 이 저장소에서 수행한다.

## 파일

- `index.html` — 대시보드 본체(원장). 외부 라이브러리·서버 없이 단독 실행. 차트는 자체 SVG.
- `README.md` — 사용자용 안내(화면 구성 / 갱신 방법 / 인센티브 기준)
- `build_artifact.py` — 게시본 빌드. 게시본의 `maskPII` 를 강제로 `true` 로 되돌리고
  갱신 도구 진입 링크를 제거한다
- `logo.png` — 사용자가 전달한 한화 심볼 원본(96px). `index.html` 에는 이 이미지가
  base64 data URI 로 박혀 있다. 로고를 다시 만들 일이 있으면 이 파일을 쓸 것.
  **직접 그린 SVG 로 대체하지 말 것** — 여러 차례 시도했으나 모두 반려됐다

## 게시된 공유 링크

https://claude.ai/code/artifact/f370a6d6-30cf-42d0-9964-b50458ad0869

같은 주소를 유지하려면 Artifact 툴에 이 URL을 `url` 인자로 넘겨 재게시한다.
`url` 없이 게시하면 **별도의 새 아티팩트**가 만들어지므로 주의.
게시 전 `<!DOCTYPE>` / `<html>` / `<head>` / `<body>` 태그를 제거한 사본을 만들어 올린다
(래퍼가 제공함). 빌드 스크립트: 저장소의 `build_artifact.py`
(`python3 build_artifact.py <출력파일>`). 마스킹 강제 로직이 들어 있어
컨테이너가 사라져도 남도록 저장소에 두었다.

**게시본과 로컬 파일의 차이는 두 가지뿐이다.**
1. 나눔고딕 웹폰트 링크
2. `maskPII` — 로컬은 `false`(고객번호 전체), 게시본은 빌드 때 `true` 로 되돌림
   빌드 스크립트에 이를 강제하는 assert 가 있으므로 제거하지 말 것.

## 주간 갱신 절차

사용자가 주간 실적 엑셀을 첨부하며 "N주차 반영"을 요청하면:

1. `openpyxl`로 엑셀 파싱 (`data_only=True`. 외부 참조 수식이 많아 캐시값을 읽어야 함)
2. `index.html`의 **DATA 블록만** 수정
   - `meta.baseDate`, `meta.updatedAt` → 데이터 기준일
   - `sep.weeks` → 해당 주차 한 줄 추가 `{ label, range, agr }`
     (9월부터 시도건수는 추출되지 않으므로 `att` 를 넣지 않는다)
   - `sep.roster` → 상담사별 `[센터, 팀, 사번, 상담사명, 참여여부(1/0), 동의건수]`
   - `sep.list` → 엑셀 `동의List` 시트
     `[주차, 날짜, 사번, 상담사명, 고객번호]`
     리스트 건수 = 주간 동의 합계 = `roster` 동의 합계 (3중 검산 포인트)
     상담사별 리스트 건수도 `roster` 동의건수와 일치해야 함
   - 실제 실적이 들어가면 `sampleMode: false` 로 변경
3. **검증** — 아래 "검증 기준" 항목을 반드시 확인
4. 커밋 → `claude/insurance-consent-dashboard-72sfr7` 브랜치에 푸시
5. 같은 URL로 아티팩트 재게시 + `SendUserFile`로 `index.html` 전달

헤더에는 버튼이 없다. 로고 · 제목 · 데이터 기준일만 둔다.
[인쇄 / PDF] · [샘플] · [메일 요약 복사] 는 모두 사용자 요청으로 제거됐으므로
되살리지 말 것. 샘플은 `sampleMode` 값으로만 전환한다.
화면 안의 이동 버튼([동의 리스트] · [← 대시보드] 등)은 `.tab` 클래스를 쓴다.

인센티브 금액·구간별 인원·1인 평균은 **입력하지 않는다.**
동의건수만 넣으면 `calcInc()`가 전부 파생 계산한다.

## 주차 데이터 갱신 도구 (`go('update')` → `viewUpdate()`)

사용자가 업무망에서 직접 주차를 갱신할 수 있도록 넣은 화면. 하단 링크로 진입한다.
외부 라이브러리 없이 동작한다. 기본은 **엑셀 파일 업로드**, 대체 수단으로
**탭 구분 텍스트 붙여넣기**(접힌 영역)를 둔다.

- `readXlsx()` / `unzip()` / `sheetToRows()` — xlsx 는 ZIP+XML 이므로
  브라우저 내장 `DecompressionStream('deflate-raw')` 로 풀고 `DOMParser` 로 읽는다.
  날짜는 스타일을 해석하지 않고 엑셀 일련번호 그대로 두어 `ymd()` 가 변환한다
  (styles.xml 파싱을 피하려는 의도적 선택).
  `DecompressionStream` 이 없는 구형 브라우저는 붙여넣기로 안내한다
- `upPick()` — 시트 이름에 기대지 않고 `ROSTER_SPEC`/`LIST_SPEC` 머리글이
  맞는 시트를 골라낸다
- `parseRoster()` / `parseList()` — **행 배열**을 받는다(파일·붙여넣기 공통 경로).
  `findCols()` 로 머리글을 찾아 열을 매핑하므로 열 순서가 바뀌어도 된다
- `weeksFrom()` — 주차 실적을 **동의 리스트에서 파생**시킨다(따로 입력받지 않음).
  덕분에 주차 합계와 리스트 건수가 구조적으로 어긋날 수 없다
- `upRun()` — 3중 검산 + 상담사 단위 대조. 모두 통과해야 저장 버튼이 나온다.
  **통과하면 곧바로 `upStage()` 로 화면 데이터에 반영한다.** 이렇게 하지 않으면
  적용을 누르지 않은 채 [엑셀 내려받기] 를 했을 때 올린 자료가 아니라
  이전 데이터가 나간다 (실제로 발생했던 문제). 파일 저장은 여전히 별도 단계
- `upSource()` — 현재 DOM 을 복제해 `#view`·`#tabs` 를 비우고 DATA 블록만
  치환한 전체 HTML 을 만들어 내려받게 한다. 생성된 파일에도 갱신 도구가 그대로 남아
  다음 주에 또 쓸 수 있다. 출력 포맷은 기존 DATA 블록과 동일하므로
  이 저장소에서 `openpyxl` 로 갱신하던 방식도 계속 쓸 수 있다
- `upSource()` 안의 문서 선언 문자열은 `'<!' + 'DOCTYPE html>'` 로 쪼개져 있다.
  빌드 스크립트가 해당 문자열 잔존 여부로 래퍼 제거를 검사하므로 합치지 말 것

## 엑셀 내려받기 (`downloadXlsx()`)

반영된 데이터를 `.xlsx` 로 내보낸다. 진입점은 **하단 링크**(모든 화면에서 보임) ·
9월 탭 · 동의 리스트 화면의 [엑셀 내려받기] 버튼과 갱신 도구의 저장 완료 안내.
사용자가 버튼을 못 찾아 하단 링크를 추가했으므로 줄이지 말 것.

- `zipStore()` — ZIP **무압축(stored)** 방식. 압축기가 필요 없어 구형 브라우저에서도
  동작한다. `crc32()` 는 직접 계산. 그래서 파일이 다소 크다(약 345KB)
- `sheetXml()` — 문자열은 `sharedStrings` 대신 `inlineStr` 로 넣어 부속 파일을 줄였다
- 시트 4장 : 월별실적 · 주간실적 · 상담사별 · 동의List
- **동의List 의 고객번호는 `maskPII` 를 따른다** (로컬=전체, 게시본=뒤 4자리).
  화면 규칙과 어긋나지 않게 할 것
- 게시본에서는 빌드 때 [엑셀 내려받기] 버튼을 제거한다.
  아티팩트 뷰어가 페이지발 내려받기를 막아 눌러도 아무 일이 없기 때문
- 검증은 `openpyxl` 로 생성 파일을 열어 확인하고, `zipfile.testzip()` 으로
  CRC 무결성까지 본다

## 9월 시상 (`go('award')` → `viewAward()`)

사용자 요청으로 추가한 화면. **탭은 자료가 없어도 항상 보인다.**
처음에는 자료가 없으면 탭을 숨기게 만들었는데, 그러면 사용자가 기능이 적용됐는지
확인할 방법이 없어 곧바로 문제가 됐다. 다시 숨기지 말 것.
자료가 없을 때는 탭에 회색 점이 붙고, 화면에는 엑셀 열 예시와
갱신 화면으로 가는 버튼이 있는 안내가 뜬다.

- 형식 : `["센터","팀","사번","상담사명", 동의누적, H-Point, 인센티브]`
- 입력은 갱신 화면의 **2번 칸(선택)** 에 시상 엑셀을 올려서 한다.
  `AWARD_SPEC` 은 `H-POINT` 열을 요구하므로 주간 실적 시트와 확실히 구분된다
- **시상 엑셀만 올려서 시상 화면만 갱신할 수 있다** (주간 자료 없이도 저장 가능).
  `upStage()` 는 주간·시상을 각각 있을 때만 덮어쓰므로 한쪽만 올려도
  다른 쪽 데이터가 지워지지 않는다
- 인센티브·H-Point 는 **엑셀 값을 그대로 쓴다** (calcInc 로 다시 계산하지 않음).
  시상은 별도 결재 결과일 수 있으므로 원본을 신뢰한다.
  대신 시상 명단 사번이 참여자 명단에 있는지는 검증한다
- 엑셀 내려받기에도 `9월시상` 시트로 포함된다
- `findCols()` 는 머리글을 대문자로 올려 비교한다(`H-POINT`/`H-Point` 모두 인식)

## 인센티브 규칙 (9월 자율참여~)

| 구간 | 기준 | 단가 |
|---|---|---|
| 1구간 | 20건 이상 | 전건 500원 |
| 2구간 | 50건 이상 | 전건 700원 |
| 3구간 | 100건 이상 | 전건 1,000원 |

- 구간 도달 시 **전 건수**에 단가 적용, 1인 월 상한 **150,000원**
- 전 구간 대상자에게 **H-Point 1,000점** 별도 지급 (`DATA.incentive.hpoint`)
- 목표 없음(자율참여). 1~8월은 시도·동의건수 **별도 시상**으로 인센티브 미적용
- 원본 엑셀 `6월 simu` 시트(총 1,224,000원 / 달성 33명)와 동일 결과로 검증된 로직

## 9월부터 달라진 집계 (2026-09 3주차~)

**9월부터는 동의건수만 추출한다.** 시도건수 · 동의율 · 시도율은 집계되지 않으므로
9월 관련 화면에서 **모두 제외**한다. 되살리지 말 것.

- 상단 KPI `전체 시도 누적건수` · `평균 동의율`, 히어로 수치, 월별 차트의 점선(동의율)과
  막대 하단(시도)은 **1~8월 기준**이며 라벨에 그렇게 표기한다
- `전체 동의 누적건수` 만 9월을 포함한다
- 월별 차트의 9월 막대는 `l2`(시도)·`r`(동의율) 을 넣지 않는다.
  `barChart()` 는 `r` 이 숫자인 항목만 동의율 선에 그린다(9월은 선에서 제외됨)
- 주간 실적 · 9월 탭의 표와 차트에는 동의건수만 둔다

## 검증 기준

- 1~8월 누계 **시도 61,471건 / 동의 11,435건** — 원본 엑셀 집계표와 일치해야 함
- 원본 `현황` 시트의 `계` 행 동의건수는 주차 반영이 늦어 실제와 어긋날 수 있다.
  **주차별 값과 `동의List` 건수를 신뢰**하고 `계` 행은 참고만 한다
  (3주차 반영 시 계 11,784 vs 실제 12,015)
- 상단 KPI·히어로의 '전체 누적'은 9월 실적이 들어오면 이를 **포함한 값**이다
  (예: 9/8 기준 시도 62,248 / 동의 11,714 / 동의율 18.8%).
  9월이 집계 중임은 섹션 부제와 차트 하단 안내로 표기한다.
  '월 평균'은 완료된 달만으로 계산해야 하므로 1~8월 기준을 유지한다
- 월별 상담사 합계 = 월 집계값 (단 6월은 동의 2,160 vs 2,159로 원본 자체가 1건 차이.
  상단 카드는 엑셀 집계표 기준, 월 탭 하단에 각주 표시)
- 주간 `weeks` 동의 누계 = `roster` 동의 합계가 되도록 맞출 것
- 브라우저 렌더링 확인 (Chromium: `executablePath:'/opt/pw-browsers/chromium'`).
  콘솔 오류 0건, 전 탭 정상 표시

## 화면 구성 (사용자가 확정한 항목 — 임의로 늘리지 말 것)

1. 전체 동의 누적건수 2. 월별 시도/동의/동의율 **차트만**(표 없음, 막대 클릭 시 월 상세)
3. 9월 주간 실적 4. 데이터 기준일 5. 인센티브 기준

**9월 센터별 실적**·**상담원 Ranking** 두 섹션은 사용자 요청으로 **제거됐다.**
되살리지 말 것. (`sepData()`의 `centers` 계산은 남아 있으나 화면에서는 쓰지 않는다)
표 머리글의 순위 칸 이름은 **`구분`** 이다(`순위` 로 되돌리지 말 것).

월 탭(1~8월)은 **엑셀 원본 내용만** 표시 — 시도/동의/동의율 + 상담사별 표.
9월 탭(`data-v="sep"` → `viewSepMonth()`)은 시도/동의/동의율 + 주차별 표 +
상담사별 표(참여여부·구간·인센티브). 헤더의 [동의 리스트] 버튼 →
`go('seplist')` → `viewSepList()` (건별 내역, 9월 탭 활성 유지).
상담사별 표에서 동의 1건 이상인 행을 클릭하면 `go('seplist:<사번>')` 으로
해당 상담사의 동의 내역만 필터링해 보여준다(주차별 미니 막대도 그 상담사 기준). 9월은 상담사별 시도건수가 없어 주차 단위로만 집계.
월별 차트의 9월 막대는 `partial:true` 로 '집계중' 표기하며 클릭 대상에서 제외한다.
응대호·미동의콜·채널별 구성 등은 사용자 요청으로 **제거된 항목**이므로 되살리지 말 것.

## 주의

- **동의 리스트에 고객 개인정보(고객번호)가 포함된다.**
  2026-09 3주차 엑셀부터 증권번호·생년월일·부문은 더 이상 제공되지 않는다.
- **현재 설정(사용자가 배포 범위를 확인하고 결정함)**
  - 로컬 `index.html` : `maskPII: false` — 고객번호 **전체 표시**.
    사내 업무용이며 화면 상단과 하단에 대외 공유 금지 경고가 뜬다.
  - 게시본(웹 링크) : 빌드 때 `maskPII: true` 로 되돌려 **뒤 4자리만** 표시.
    링크는 누구에게 전달될지 통제할 수 없으므로 이 원칙을 바꾸지 말 것.
  - 이 구성을 되돌리거나 게시본까지 전체 표시로 바꾸려면 **사용자에게 먼저 확인**한다.
- 상담사 실명·사번 262명분이 포함됨. 공유 범위 확대 시 사용자에게 확인할 것.
  GitHub Pages 등 완전 공개 배포는 권하지 않는다.
- 아티팩트는 기본 비공개. 공개 범위는 사용자가 페이지 공유 메뉴에서 직접 설정해야 한다.
